// Generated by Selenium IDE
import org.junit.Test;
import org.junit.Before;
import org.junit.After;
import static org.junit.Assert.*;
import static org.hamcrest.CoreMatchers.is;
import static org.hamcrest.core.IsNot.not;
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.firefox.FirefoxDriver;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.remote.RemoteWebDriver;
import org.openqa.selenium.remote.DesiredCapabilities;
import org.openqa.selenium.Dimension;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.interactions.Actions;
import org.openqa.selenium.support.ui.ExpectedConditions;
import org.openqa.selenium.support.ui.WebDriverWait;
import org.openqa.selenium.JavascriptExecutor;
import org.openqa.selenium.Alert;
import org.openqa.selenium.Keys;
import java.util.*;
import java.net.MalformedURLException;
import java.net.URL;
public class TC002VerifyaddingproducttocartTest {
  private WebDriver driver;
  private Map<String, Object> vars;
  JavascriptExecutor js;
  @Before
  public void setUp() throws MalformedURLException {
    driver = new RemoteWebDriver(new URL("http://localhost:4444/wd/hub"), DesiredCapabilities.chrome());
    js = (JavascriptExecutor) driver;
    vars = new HashMap<String, Object>();
  }
  @After
  public void tearDown() {
    driver.quit();
  }
  @Test
  public void tC002Verifyaddingproducttocart() {
    // Test name: TC002_Verify adding product to cart
    // Step # | name | target | value
    // 1 | open | / | 
    driver.get("https://www.flipkart.com//");
    // 2 | setWindowSize | 1536x816 | 
    driver.manage().window().setSize(new Dimension(1536, 816));
    // 3 | click | css=.\_30XB9F | 
    driver.findElement(By.cssSelector(".\\_30XB9F")).click();
    // 4 | mouseOver | linkText=Appliances | 
    {
      WebElement element = driver.findElement(By.linkText("Appliances"));
      Actions builder = new Actions(driver);
      builder.moveToElement(element).perform();
    }
    // 5 | click | name=q | 
    driver.findElement(By.name("q")).click();
    // 6 | runScript | window.scrollTo(0,0) | 
    js.executeScript("window.scrollTo(0,0)");
    // 7 | type | name=q | LAPTOP
    driver.findElement(By.name("q")).sendKeys("LAPTOP");
    // 8 | sendKeys | name=q | ${KEY_ENTER}
    driver.findElement(By.name("q")).sendKeys(Keys.ENTER);
    // 9 | close |  | 
    driver.close();
  }
}
